# CLAUDE.md

株式会社丸翔（丸翔リフォーム／福島県いわき市）の公式サイトのリポジトリです。
このファイルは、AI アシスタントがこのリポジトリで作業するための手引きです。

---

## 1. このプロジェクトの性質

**ビルド工程のない静的サイトです。** package.json・node_modules・CI 設定・テストは
一切ありません。HTML / CSS / 素の JavaScript を直接編集し、そのまま公開されます。

- npm install / ビルド / テストコマンドは存在しません。実行しようとしないでください。
- 動作確認はブラウザで `index.html` を開くだけです。
  （ローカル確認では GA4・Clarity は動きません。§5 の理由により意図的な仕様です）
- 依存ライブラリはありません。jQuery も React もフレームワークも使っていません。
  新しいライブラリを持ち込まないでください。

**公開先**：GitHub Pages。`main` ブランチの内容がそのまま
`https://marusho-iwaki.co.jp/` として公開されます（独自ドメインは `CNAME` で指定）。
`main` へのマージ＝即本番反映です。

**作業ブランチ**：作業は指定された作業ブランチで行い、`main` へ直接コミットしないでください。

---

## 2. ファイル構成

| ファイル / ディレクトリ | 役割 |
| --- | --- |
| `index.html` | トップページ（全セクション＋ページ末尾のインライン JS。約 1,100 行） |
| `coupon.html` | 5,000円特別割引券ページ（`noindex`。localStorage で使用済み管理） |
| `privacy.html` | プライバシーポリシー |
| `style.css` | 全ページ共通の CSS（約 800 行。構成は §4 参照） |
| `news.js` | **お知らせのデータ**（`NEWS_ITEMS` 配列） |
| `cases.js` | **施工事例のデータ**（`CASE_ITEMS` 配列） |
| `analytics.js` | 電話タップ・割引券表示の計測、`window.msTrack` の提供 |
| `CNAME` | 独自ドメイン `marusho-iwaki.co.jp` |
| `robots.txt` / `sitemap.xml` | 検索エンジン向け設定（§6 参照） |
| `lp_ui_assets_png/` | UI パーツ（`brand/` `icons/` `buttons/` `decorations/` `preview/`） |
| `lp_image_assets_png/` | 写真素材（`cases/web/` `slideshow/` `news/`） |

`index.html` のセクション id：
`#top` `#cases` `#voice`(非表示) `#news` `#service` `#cta` `#about` `#philosophy`
`#pricing` `#faq` `#support` `#contact` `#access` `#company`

---

## 3. コンテンツの更新方法（最重要）

### お知らせ・施工事例は HTML を触らない

`index.html` の「お知らせ」「施工事例」は空の `<div>` で、ページ末尾のインライン JS が
`news.js` / `cases.js` の配列から HTML を生成しています。

- **お知らせを追加・修正 → `news.js` の `NEWS_ITEMS` だけを編集**
- **施工事例を追加・修正 → `cases.js` の `CASE_ITEMS` だけを編集**

両ファイルとも冒頭に、依頼者本人が読むことを想定した日本語の詳細な手引きコメントが
書かれています。**このコメントは仕様書です。編集時は必ず読み、削らないでください。**

要点：

- 表示順は**配列に書いた順**（上から）。`date` は表示されるだけで並び順に影響しません。
- お知らせはトップに最大 8 件表示（`index.html` の `NEWS_MAX`）。
- 日付は必ず `"2026-07-01"` 形式。
- リンクが 1 つなら `link` + `linkText`（カード全体がリンク）、
  2 つ以上なら `links: [{url, label}, …]`（各行が個別リンク）。両方は併用しません。
- `.pdf` には自動で「PDF」バッジ、`http(s)://` には自動で「↗」が付きます。
  サイト内 `.html` は同じタブ、それ以外は別タブで開きます。

### 事例写真の置き場所

- Web 用に軽量化した 4:3・800×600 を `lp_image_assets_png/cases/web/` に置きます。
- 命名は `<題材>_before.jpg` / `<題材>_after.jpg`。
- 高解像度の元写真はリポジトリには入っていません（`cases.js` のコメントにある
  `lp_image_assets_png/cases/` 直下は現在コミットされていません）。

### レンダリング側を触るときの注意

`renderNews()` / `renderCases()` は文字列連結で HTML を組み立てています。
**データ由来の値は必ず `esc()` を通してください。** 素通しすると XSS になります。

---

## 4. CSS の構造

`style.css` は**2 層構造**です。

1. 前半：基本スタイル（`:root` のカラー変数 → Reset → Buttons → 各セクション → Responsive）
2. 485 行目付近の `/* ここから：改善提案書（改善前後比較）対応の追加スタイル */` 以降：
   後から重ねた**上書きレイヤー**（Hero・Stats・お知らせカード・FAQ・費用・
   サポート・割引券ページ・スマホ調整など）

前半のルールを直しても後半に打ち消されていることがあります。**必ず両方を検索してから**
編集し、上書きレイヤーに属するセクションの調整は後半側で行ってください。

カラー変数（`:root`）：
`--orange:#e8821e` / `--orange-dark:#d9741a` / `--teal:#0e3d34`（濃緑・ヘッダー/Stats）/
`--navy:#14314d` / `--cream:#f5efe4` / `--line:#e3ddd2` / `--text:#333` / `--muted:#6b6b6b`

ブレークポイントは `1024px` / `768px` / `600px` / `420px` の max-width。
`prefers-reduced-motion` への配慮も入っています（Hero）。

---

## 5. 計測（GA4 / Clarity）

- GA4 測定 ID：`G-WCEB8Y4N4F`
- Microsoft Clarity プロジェクト ID：`xvefvdpdvg`

**本番ドメインでのみ計測します。** 各 HTML の `<head>` に
`if (location.hostname === 'marusho-iwaki.co.jp')` のガードがあり、localhost や
`file://` での表示確認がレポートに混ざらないようにしています。**このガードを外さないでください。**

計測タグは `index.html` / `coupon.html` / `privacy.html` の 3 ファイルに**同じ内容が
重複**しています。測定 ID やドメインを変更するときは **3 ファイルすべて**と、
必要に応じて `analytics.js` を直してください。

`analytics.js` が記録するイベント：

| イベント | 発火 |
| --- | --- |
| `tel_tap` | `a[href^="tel:"]` のクリック（document 委譲。`link_location` に位置ラベル） |
| `contact_submit` | フォームのサーバー送信成功時（`index.html` から `window.msTrack` 経由） |
| `contact_mailto` | mailto フォールバック送信時 |
| `coupon_view` | `coupon.html` の表示 |

電話リンクは document 側でまとめて監視しているため、リンクを増やしても計測は漏れません。
ただし位置ラベルは `analytics.js` の `TEL_PLACES`（セレクタ → 日本語ラベル）で判定するので、
**新しいコンテナに電話リンクを置いたら `TEL_PLACES` にも追加**してください（未登録は「その他」）。

計測イベントに個人情報（氏名・電話番号・住所・本文）を送らないでください。
現在送っているのは相談内容・工事内容・きっかけの選択値だけです。

---

## 6. SEO（robots.txt / sitemap.xml）

**「検索に載せたくない」ときは `Disallow` ではなく `noindex` を使ってください。**
`Disallow` にするとクローラがページを読めず `noindex` に気づけないため、
URL だけが検索結果に残ることがあります。この方針は `robots.txt` 内にも明記されています。

- `coupon.html` は `<meta name="robots" content="noindex">` で除外しています。
- `sitemap.xml` には検索に載せたいページだけを書きます（現在は `/` と `/privacy.html`）。
  ページを追加・削除したら **`sitemap.xml` の一覧と `lastmod` も更新**してください。
- `robots.txt` / `sitemap.xml` のコメントにある `before/` `review/` は、
  過去に存在した改善前サイト・校正用ページで、コミット `fa6bfd8` で削除済みです。

`index.html` には JSON-LD が 2 つあります（`HomeAndConstructionBusiness` と `FAQPage`）。
**会社情報・FAQ の本文を直したら、対応する JSON-LD も同じ内容に直してください。**

---

## 7. お問い合わせフォーム

`index.html` 末尾のインライン JS が処理しています。

- 送信先：SSGform（`FORM_ENDPOINT = 'https://ssgform.com/s/2qEq3hthPZK0'`）
- `FORM_ENDPOINT` を `''` にすると、メールソフト起動（mailto）方式に自動で切り替わり、
  ボタン下の注意書きも切り替わります。フォールバック宛先は
  `RECIPIENT = 'yamauchi310lyd@gmail.com'`。

触るときの注意：

- **`X-Requested-With: XMLHttpRequest` ヘッダを消さないでください。** SSGform はこれで
  Ajax 送信を判定します。無いと転送用 HTML が返り、送信できているのに失敗扱いになります。
- 件名・返信先は SSGform の管理画面側の設定です。`_subject` / `_replyto` は送りません。
- `FormData` のキーは**日本語の項目名**です（通知メールをそのまま読めるようにするため）。
  項目を増やすときも日本語キーで揃えてください。
- `_gotcha` は迷惑メール対策の隠し欄です。入力があれば静かに中断します。削除しないでください。
- 二重送信は `btn.dataset.sending` で防いでいます。
- 必須項目・メール形式の検証はクライアント側で行い、エラー行に `.has-error` を付けます。

---

## 8. 割引券ページ（coupon.html）

スマホで開いて店頭提示し、「使用する」を押すと使用済みスタンプが押される仕組みです。

- 使用済み情報は **localStorage（`marusho_coupon5000_used_v1`）にのみ**保存されます。
  サーバー管理ではないので、ブラウザを変えれば再利用できます（仕様として許容）。
- 券の内容を変えて全員に再配布したいときは、キーの `v1` を `v2` に上げます。
- `?reset=1` を付けて開くと未使用に戻せます（店頭でのテスト用）。
- localStorage が使えない環境（プライベートモード等）でもページが壊れないよう
  try/catch で握りつぶし、保存できなかった場合は画面に案内を出します。
- モーダルは、スマホのタッチ＋マウス二重イベント対策として開いてから 400ms は
  背景タップを無視します。

---

## 9. 触ってはいけない「意図的にそうなっている」箇所

コード内のコメントで明示されているものです。良かれと思って直さないでください。

1. **地図の住所が「8-1」なのは正しい。**
   表示用の住所は登記どおり `好間町中好間鍛冶内8番地`、Google マップの URL
   （`q=` と経路案内の `destination=`）だけ `8-1` にしています。「8番地」では
   マップが正しい位置を示さないためです。
2. **「お客様の声」セクションは意図的に非表示。**
   掲載許可の確認が取れるまで、`index.html` の `#voice` セクションとナビの該当リンクを
   コメントアウトしています（2026-07-28 丸翔さまご判断）。復活させるのは許可が
   取れてからで、あわせて掲載地域が対応エリア（いわき市）と合っているかの確認が必要です
   （現在のダミーには郡山市・福島市が含まれています）。
3. **資格・許認可／工事保険はフッターに載せない**という判断です（2026-07-28）。
   掲載する場合の書式だけコメントで残してあります。
4. **数値には注記（※）が必ず対になっています。**
   「年間施工実績 120件」「最長10年保証」には Stats 直下の `.stats-note` と FAQ に
   根拠・免責の説明が付いています。数値だけ動かしたり、注記だけ消したりしないでください。
5. **計測タグのドメインガード**（§5）と **SSGform の `X-Requested-With`**（§7）。

---

## 10. 確定している事実情報

サイト全体で表記を揃える必要があります。

- 会社名：株式会社 丸翔（丸翔リフォーム）／代表取締役 山内 寿徳
- 所在地：〒970-1152 福島県いわき市好間町中好間鍛冶内8番地
- TEL：代表 0246-84-5557 ／ 携帯 090-2025-3104　FAX：0246-84-5571
- E-mail：yamauchi310lyd@gmail.com
- 営業時間 10:00〜18:00 ／ 定休日 火曜日
- 設立 2007年／年間施工実績 120件（2025年1月〜12月の完工件数）
- 対応エリア：福島県いわき市全域

電話番号を変更するときは、`tel:` リンク（ヘッダー・Hero・CTA・お問い合わせ・
会社概要・スマホ追尾バー）、表示テキスト、JSON-LD、フォームのエラーメッセージ、
`coupon.html` / `privacy.html` のヘッダーまで、**全ファイルを横断して**直してください。

---

## 11. 執筆・コミットの作法

- **サイトの文言・コード内コメントはすべて日本語。** コメントは開発者向けではなく、
  **依頼者（丸翔さま）が自分で更新できるようにするための説明**として書かれています。
  この文体（丁寧・平易・具体的な手順）を踏襲してください。
- **コミットメッセージも日本語**で、技術用語ではなく**何が良くなるか**を書きます。
  既存例：
  - `施工事例に「橋の拡張・補修工事」「外壁塗装（クリーム色）」を追加`
  - `本番ドメイン以外では計測しないようにし、検証用アクセスがレポートに混ざるのを防ぐ`
  - `独自ドメイン marusho-iwaki.co.jp を設定し、所在地を登記どおり「8番地」に修正`
- 広告表現には注意が必要です。実績・保証・補助金に関する記述には、必ず根拠と
  免責の注記を添えてください（景品表示法・特定商取引法上のリスク回避のため）。
- 補助金など外部制度の案内には「情報確認日」を添え、必ず自治体公式ページへのリンクを
  併記してください（現在の確認日：2026年7月17日）。

---

## 12. 変更後のチェックリスト

ビルドもテストもないぶん、目視確認が唯一の検証です。

- [ ] `index.html` をブラウザで開き、お知らせ・施工事例カードが表示されるか
      （`news.js` / `cases.js` の構文エラーは、カードが**丸ごと消える**形で現れます。
      Console にエラーが出ていないか必ず確認）
- [ ] スマホ幅（〜768px）でレイアウトが崩れていないか、追尾バーが被っていないか
- [ ] 新規・変更した画像パスが実在するか（404 になっていないか）
- [ ] 電話リンク・CTA・アンカーリンクが正しく飛ぶか
- [ ] 文言を変えた場合、JSON-LD・sitemap の `lastmod`・重複している head ブロックの
      追従漏れがないか
